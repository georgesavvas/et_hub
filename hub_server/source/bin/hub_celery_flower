#!/bin/bash

SCRIPT_DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )

cd $SCRIPT_DIR/../python;
source ../../env/bin/activate;
source ../bin/env;
celery -A hub_server.celery flower --broker="amqp://${RABBITMQ_URL}"
